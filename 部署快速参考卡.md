# 部署快速参考卡（一页纸）

> 配合《部署操作手册-Render版.md》使用，关键信息速查

---

## 🚀 三步上线

```
① Gitee 上传代码  →  ② Supabase 建库+导入SQL  →  ③ Render 创建服务
```

---

## 📌 三个平台

| 平台 | 地址 | 用途 |
|------|------|------|
| Gitee | https://gitee.com | 代码托管 |
| Supabase | https://supabase.com | PostgreSQL数据库 |
| Render | https://render.com | 应用部署 |

---

## ⚙️ Render 配置速查

### 基本设置
| 配置项 | 值 |
|--------|-----|
| Name | `gongzi-info` |
| Region | `Singapore` |
| Runtime | `Docker` |
| Dockerfile Path | `Dockerfile`（根目录） |
| Branch | `master` 或 `main` |
| Plan | `Free`（免费）或 `Starter`（$7/月） |
| Health Check Path | `/health` |

### 环境变量（必须）
| Key | Value |
|-----|-------|
| `DATABASE_URL` | Supabase 的 URI 连接串 |
| `SECRET_KEY` | 随机32位字符串 |
| `APP_ENV` | `production` |
| `CORS_ORIGINS` | `*` |

### 环境变量（可选）
| Key | Value | 说明 |
|-----|-------|------|
| `AI_API_KEY` | `sk-xxx` | 不填用模拟模式 |
| `AI_BASE_URL` | API地址 | 如 DeepSeek 等 |
| `AI_MODEL` | 模型名 | 如 `deepseek-chat` |

---

## 🔑 Supabase 连接串格式

```
postgresql://postgres:你的密码@db.xxxx.supabase.co:5432/postgres
```

获取路径：**Project Settings → Database → Connection string → URI**

---

## 📊 数据库初始化

1. Supabase → **SQL Editor** → New query
2. 粘贴 `deploy/init.sql` 全部内容
3. **Run** 执行
4. （可选）Render Shell 中执行 `python init_db.py` 插入示例数据

---

## ✅ 验证清单

部署成功后依次访问：

- [ ] `https://xxx.onrender.com/health` → 返回 `{"status":"ok"}`
- [ ] `https://xxx.onrender.com/docs` → Swagger 文档
- [ ] `https://xxx.onrender.com` → 前端登录页

---

## 👤 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | `admin` | `admin123` |
| 普通用户 | `user` | `user123` |

⚠️ **首次登录立即修改密码！**

---

## ❓ 常见问题速查

| 问题 | 解决方案 |
|------|----------|
| 构建失败 npm 错误 | package.json build 去掉 `vue-tsc &&` |
| 页面空白 | 确认用根目录 Dockerfile，非 backend/Dockerfile |
| 数据库连不上 | 检查 DATABASE_URL，Supabase IP 白名单设 0.0.0.0/0 |
| 访问慢/休眠 | 免费版15分钟休眠，升级 Starter 或用监控保活 |
| 更新代码 | 推送到 Gitee，Render 自动重新部署 |

---

## 💰 成本

| 方案 | 月费 | 说明 |
|------|------|------|
| 全免费 | ¥0 | 会休眠，适合试用 |
| 低成本正式 | ~¥230 | Render Starter + Supabase Pro |

---

*打印此页，部署时对照操作*
